# 更新日志

## [0.1.7] - 2026-08-22

约 25 处 UI 与逻辑缺陷修复（外部 PR 合并）🔧

### 修复
- **Kraljic 矩阵**：四象限布局与业务计算一致（左上杠杆/右上战略/左下常规/右下瓶颈），落点与品类徽章颜色不再矛盾；落点补无障碍标签
- **AI 对话**：修复首次发送竞态（模型加载完成后再发送初始提示）；支持多轮对话上下文；关闭后重开不残留旧对话；模型为空时 modelId 置空
- **数据导入/备份**：修复外键错乱与数据重复；`deleteSupplier`/`deleteCategory` 事务级联清理孤儿数据；CSV 解析修复英文表头、未加引号字段内引号导致列错位、小数坐标
- **Android 体验**：修复 AI 设置子页按返回键直接退出应用问题；Modal 内容区补底部安全区内边距
- **AI 设置**：空 API Key 拦截保存并提示；保存/删除 Key、删除/添加模型失败均有 toast
- **基础组件**：Chip 非激活边框可见；Toast 关闭动画回调优化；Modal 返回键监听正确移除
- **屏幕逻辑**：洞察删除前加二次确认；行动项状态切换失败有提示；行动项截止日期非法格式拦截；星级越界渲染钳制；供应商详情加载失败不崩溃
- **日期工具**：`relativeTime`「昨天/前天」改按日历日判定

## [0.1.6] - 2026-08-22

数据批量导入 + AI 模型二级页 + 交互/弹窗优化 🚀

### 新增
- **CSV 批量导入（数据管理）」**：供应商与品类支持直接导入 CSV 表格，自动识别表头（名称/编码/品类/分级/状态/联系人/电话/邮箱/备注，品类为 名称/供应风险/采购影响/战略/备注），支持引号包裹字段与逗号/换行；供应商未知品类自动创建、中文分级/状态自动映射、重复名称跳过
- **AI 模型独立二级管理页**：从「我的 → 管理模型与 API Key」进入，集中管理已配置模型、配置 Key、添加自定义模型、删除模型

### 优化
- **MiniMax 常用模型预设**：内置 MiniMax，并修正其 OpenAI 兼容端点为国内可达的 `api.minimax.chat`（修复配置后调用报错：原国际域名 `api.minimaxi.com` 国内不可达）
- **调研页类型筛选**：顶部类型标签由竖向长条改为紧凑胶囊 + 自动换行，布局更整洁不占高度
- **所有弹出页面全屏化**：Modal 改为全屏页（顶部 ‹ 返回 + 居中标题），统一 Android 16 沉浸式交互与返回手势

## [0.1.5] - 2026-08-21

首页 & 调研页 UI 打磨（基于 minimax-vision 视觉审查）🎨

### 修复/优化
- **首页「快速记录」布局**：4 枚标签换行后不再与「记录」按钮错位相撞；按钮改为整行宽主按钮，独立置于标签下方，对齐清晰稳定
- **首页统计卡节奏**：三卡的图标/数字垂直光学居中、间距统一，卡片等高对齐
- **调研卡信息层级**：正文与下方供应商/星级/时间行距加大；星号评分与时间用 `·` 明确分组；「结论」气泡增加左侧靛蓝竖条 + 加粗文字，强调度与可读性提升

## [0.1.4] - 2026-08-21

UI 全面检查 + Android 16 设计规范合规（修复 / 可达性）🔧

### 修复
- **Kraljic 矩阵拉伸变形**：`viewBox` 原本是正方形却被注入宽矩形（`width=100% / height=300`），导致品类落点圆形被水平拉成椭圆；改为等比容器渲染，落点恢复正圆、象限保持正方形
- **Toast 顶部遮挡**：`top` 硬编码 `64`，未走安全区，刘海/挖孔屏可能被系统栏遮挡；改为基于 `useSafeAreaInsets().top`

### 合规优化（Android 16 / Material 触摸与无障碍）
- **触摸热区达标**：`Chip`、`AppButton`、评分单元格、详情操作按钮、AI 发送键等提升至 44–48dp，删除键/风险状态键补 `hitSlop`，符合 Android 触摸目标建议
- **文本对比度达标**：`textTertiary` 令牌深色化（`#9E9E9E → #757575`），浅色背景辅助/占位文本达到 WCAG AA（≥4.5:1）
- **无障碍语义**：`TabBar` 补 `tab` 角色与选中态、`Chip`/`AppButton` 补 `button` 角色、复选框补 `checkbox` 角色与勾选态、关键图形按钮（AI/统计/删除）补 `accessibilityLabel`、API Key 输入补标签；底部弹窗声明 `accessibilityViewIsModal` 模态 + `statusBarTranslucent` 适配 edge-to-edge
- **edge-to-edge 收口**：随 Expo 56 系统强制边到边，各屏 `SafeAreaView`/`TabBar`/弹窗已正确使用安全区；系统栏图标由 `expo-status-bar` 深色化

## [0.1.3] - 2026-08-20

UI 代码质量与视觉一致性优化 🎨

### 新增
- 可复用 UI 基元组件 `ui.tsx`：`Card` / `Chip` / `AppButton` / `FieldLabel` / `SectionHeader` / `EmptyState`，作为设计令牌单一来源

### 优化
- **消除跨屏样式重复**：5 个屏幕原各自重复定义的 `card` / `fieldLabel` / `fieldInput` / `chip` / `saveBtn` / `empty` 等样式统一抽到基元组件，仅保留屏幕特有布局
- **底部弹窗加固**（Modal）：统一高度上限到 85% 屏高（消除与 maxHeight 的双重冲突）、按底部安全区留白、键盘弹出时主按钮可见、内容可滚动
- **Kraljic 矩阵可读性**：内容限定安全区，纵向轴标注用旋转定位避免裁切，象限/图例更清晰
- **节标题/空状态统一**：节标题改用 `SectionHeader`、空状态改用 `EmptyState`，全屏一致

## [0.1.2] - 2026-08-20

Bug 修复 + 商务风 UI 质感升级 ✨

### 修复

- **数据备份导入丢失调研模板**：替换/合并导入均不再恢复 `research_templates`，现已修复（替换按备份重建、合并仅补齐不重复模板）
- **行动项/风险删除不安全**：之前删除无确认、未 await、无错误处理，误触即丢数据；现补齐确认弹窗 + await + 失败提示，与供应商/品类/调研删除行为一致

### 优化

- 新增统一设计令牌 `SHADOW` / `SHADOW_PRIMARY`，为卡片、统计卡、列表项、主按钮补上柔和投影与立体感，强化商务质感
- 底部 TabBar 激活态升级：选中项加靛蓝浅底 + 白底图标药丸，替代单一小圆点，辨识度更高
- 顶部「新增 / AI 助手 / 保存」等主按钮统一带轻度投影，层次更分明

## [0.1.1] - 2026-08-15

S25 / Android 16 / One UI 8.5 适配优化 📱

### 修复

- 底部标签栏适配手势导航条（`useSafeAreaInsets`），不再被系统手势条遮挡
- 底部弹窗高度自适应屏幕（上限 85%），S25 等小屏不再溢出
- Kraljic 矩阵品类落点加大 + 透明触摸热区，更易点击

### 优化

- 新增按钮触摸目标提升至 44dp（接近 Material 3 的 48dp 标准）
- 对齐 edge-to-edge 显示，顶部状态栏/挖孔、底部手势条均正确留白

## [0.1.0] - 2026-08-15

首个版本发布 🎉

### 新增

- **供应商管理**：档案（名称/编码/品类/分级/状态/联系人）+ 五级分级 + 一页画像详情
- **品类战略**：Kraljic 矩阵四象限可视化 + 战略沉淀 + 象限策略提示
- **供应商调研**：记录调研问题/发现/评分/结论，预置 3 套 CPSM 调研模板
- **战略洞察**：首页快捷记录 + 标签分类 + 全局时间线
- **AI 智能助手**：内置 6 家模型模板（DeepSeek/通义/智谱/Kimi/OpenAI/Ollama）+ 自定义模型接入 + API Key 加密存储
- **风险登记册**（PMP）：概率×影响评分 + 应对策略
- **行动项**：待办管理 + 责任人 + 勾选完成
- **数据备份**：JSON 导出/导入（合并/替换）

### 技术

- Expo SDK 56 + React Native 0.85 + TypeScript strict
- expo-sqlite 本地存储 + expo-secure-store 加密
- 设计延续 TapMood 暖色家族，主色深靛蓝体现商务专业感
